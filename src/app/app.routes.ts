import { Routes } from '@angular/router';
import { NavComponent } from './project/layout/header/nav/nav.component';

export const routes: Routes = [
    {
        path: '',
        component: NavComponent  // Default route

    },
];
